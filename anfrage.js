const form=document.getElementById('quote-form');const btn=document.getElementById('quote-submit');const statusBox=document.getElementById('quote-status');if(form){form.addEventListener('submit',async e=>{e.preventDefault();btn.disabled=true;btn.textContent='Wird gesendet …';statusBox.textContent='Anfrage wird sicher übertragen …';try{const response=await fetch(form.action,{method:'POST',body:new FormData(form),headers:{Accept:'application/json'}});const data=await response.json().catch(()=>({}));if(!response.ok||data.success===false)throw new Error('send_failed');statusBox.textContent='✓ Anfrage wurde gesendet. Wir melden uns mit dem Kostenvoranschlag.';form.reset();}catch(err){statusBox.textContent='Senden fehlgeschlagen. Bitte später erneut versuchen oder direkt per E-Mail Kontakt aufnehmen.';}finally{btn.disabled=false;btn.textContent='Anfrage senden';}});}